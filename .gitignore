# forjar state — ephemeral apply output
**/state/
state/
*.lock
.forjar/

# /tmp convergence target for labs
/tmp/

# Local pmat / pv tooling state
/.pmat/
/.pmat-metrics/
/.pv/
.pmat/context.db
.pmat/context.db-shm
.pmat/context.db-wal
.pmat/context.idx/

# Rust (none expected, but defensive)
/target/
target/
**/*.rs.bk

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl

# OS
.env
.DS_Store
*.swp

# Repo-local plan output (Makefile TMPDIR)
.tmp/

# hello-world demo state (created by `make hello`)
hello/world/
hello/state/
hello/forjar.lock.yaml
hello/forjar.lock.yaml.b3

# IDE state
.idea/
